# name: Déploiement vers Heroku

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.12"

#       - name: Install Heroku CLI
#         run: |
#           curl https://cli-assets.heroku.com/install.sh | sh

#       - name: Login to Heroku
#         env:
#           HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
#         run: |
#           echo "$HEROKU_API_KEY" | heroku auth:token

#       - name: Add Heroku remote
#         env:
#           HEROKU_APP_NAME: ${{ secrets.HEROKU_APP_NAME }}
#         run: |
#           heroku git:remote -a $HEROKU_APP_NAME

#       - name: Deploy to Heroku
#         run: |
#           git push heroku main --force







# name: Déploiement vers Heroku

# on: 
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Deploy to Heroku
#         uses: akhileshns/heroku-deploy@v3.12.12
#         with:
#           heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
#           heroku_app_name: ${{ secrets.HEROKU_APP_NAME }}
#           heroku_email: ${{ secrets.HEROKU_EMAIL }}




# name: Déploiement vers Heroku

# on: 
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Deploy to Heroku
#         uses: akhileshns/heroku-deploy@v3.12.12
#         with:
#           heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
#           heroku_app_name: ${{ secrets.HEROKU_APP_NAME }}
#           heroku_email: ${{ secrets.HEROKU_EMAIL }}
#           dontautocreate: true



# name: Déploiement vers Heroku

# on: 
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Install Heroku CLI
#         run: |
#           curl https://cli-assets.heroku.com/install.sh | sh
#           heroku --version

#       - name: Authenticate Heroku
#         env:
#           HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
#         run: |
#           echo "$HEROKU_API_KEY" | heroku auth:token
#           heroku auth:whoami

#       - name: Add Heroku remote
#         run: |
#           heroku git:remote -a "${{ secrets.HEROKU_APP_NAME }}"

#       - name: Deploy to Heroku
#         run: |
#           git push heroku HEAD:main --force



# name: Deploy to Heroku (Working)

# on: 
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Install Heroku CLI
#         run: |
#           curl https://cli-assets.heroku.com/install.sh | sh
#           heroku --version

#       - name: Configure Heroku authentication (netrc)
#         env:
#           HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
#         run: |
#           cat > ~/.netrc <<EOF
#           machine api.heroku.com
#             login _
#             password $HEROKU_API_KEY
#           machine git.heroku.com
#             login _
#             password $HEROKU_API_KEY
#           EOF
#           chmod 600 ~/.netrc

#       - name: Verify Heroku access
#         run: |
#           heroku apps

#       - name: Add Heroku remote
#         run: |
#           heroku git:remote -a "${{ secrets.HEROKU_APP_NAME }}"
#           git remote -v

#       - name: Deploy to Heroku
#         run: |
#           git push heroku HEAD:main --force


name: Deploy to Heroku (Auto on main)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # important pour Heroku

      - name: Install Heroku CLI
        run: |
          curl https://cli-assets.heroku.com/install.sh | sh
          heroku --version

      - name: Configure Heroku authentication (netrc)
        env:
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
        run: |
          cat > ~/.netrc <<EOF
          machine api.heroku.com
            login _
            password $HEROKU_API_KEY
          machine git.heroku.com
            login _
            password $HEROKU_API_KEY
          EOF
          chmod 600 ~/.netrc

      - name: Verify Heroku access
        run: |
          heroku apps

      - name: Add Heroku remote
        run: |
          heroku git:remote -a "${{ secrets.HEROKU_APP_NAME }}"
          git remote -v

      - name: Deploy to Heroku
        run: |
          git push heroku HEAD:main --force
